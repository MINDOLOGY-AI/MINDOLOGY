# block label  
# this should be pre SAEs. we just study the entire activation space as a whole and don't try to decompose it.  
# we will divide the entire activation space into blocks. idk. maybe differing by like 0.01 at each coordinate  
# as we run over the entire training corpus, we maybe write a block_to_context.json linking block num to context num. and kee[
